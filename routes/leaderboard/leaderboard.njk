{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('leaderboard.title') }}</h1>

    <div>
        <p>
            {% for result in results%}
                <strong>{{result.id}}</strong> {{result.percentageLoss}}<br/>
            {% endfor %}
        </p>
    
    </div>

{% endblock %}
